import 'package:flutter/material.dart';

class WorkspacesWidget extends StatelessWidget {
  const WorkspacesWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Workspaces');
  }
}
